<UserControl x:Class="AptekaInternetApp.View.MainWindowFile.UsersControls.CatalogCategoriesUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AptekaInternetApp.View.MainWindowFile.UsersControls"
             mc:Ignorable="d" 
                                 d:DesignHeight="450" d:DesignWidth="1000">
    <StackPanel Background="White" Orientation="Horizontal">
        <ItemsControl x:Name="ItemsControlCategories" Height="700" HorizontalAlignment="Center">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Click="CategoryButton_Click" Tag="{Binding}" 
                            Style="{StaticResource CategoryButtonStyle}"
                            Margin="10,5,10,5">
                        <Border HorizontalAlignment="Center" BorderBrush="#30a8ff" BorderThickness="0 0 0 2">
                            <StackPanel>
                                <TextBlock FontSize="20" Text="{Binding Name}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</UserControl>
