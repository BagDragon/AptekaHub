<UserControl x:Class="AptekaInternetApp.View.LoginAndRegWindows.UserControls.RequestEmailUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="InputField" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#E5E5E5"/>
            <Setter Property="BorderThickness" Value="1"/>
            
            <Setter Property="Padding" Value="15"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#30a8ff"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <StackPanel VerticalAlignment="Center">
        <TextBlock Text="🔐" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,20"/>
        <TextBlock Text="Восстановление доступа" Foreground="#333333" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
        <TextBlock Text="Введите email для отправки кода" Foreground="#666666" HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <TextBox Name="EmailTextBox" Style="{StaticResource InputField}" 
                 Margin="0,0,0,20"/>

        <Button Name="SendEmailButton" Style="{StaticResource PrimaryButton}" 
                Content="Продолжить" Click="SendEmailButton_Click"/>
    </StackPanel>
</UserControl>
